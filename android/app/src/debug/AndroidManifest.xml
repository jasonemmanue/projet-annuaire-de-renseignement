<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission Internet requise pour Flutter hot-reload et Firebase -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--
      Active la config réseau debug : autorise HTTP cleartext vers
      localhost / 127.0.0.1 / 10.0.2.2 pour l'émulateur Firebase Auth.
      Sans effet sur les builds release.
    -->
    <application
        tools:node="merge"
        android:networkSecurityConfig="@xml/network_security_config" />

</manifest>
